# Purpose Directory (comment-only)
# res://AGENTS.md - Persistent AI project directives: use FILE_DIRECTORY, keep it updated, preserve typed GDScript, follow repository change discipline, and warn before editing .tscn files so unsaved Godot editor changes can be saved first.
# res://BACKLOG.md - Unfinished, actionable project work only; completed items and implementation history do not belong here.
# res://docs/SYSTEMS.md - Durable description of current system behavior, responsibilities, and cross-system constraints without duplicating volatile scene tuning values.
# res://docs/DECISIONS.md - Concise architectural decisions that future work should not reverse without reevaluation.
# res://autoload/DialogueManager.gd - Load dialogue text, own editable default bubble duration/fade timing, instantiate the configured TextBubble scene, show and retarget queued/active text bubbles, optionally spawn detached duplicate effect bubbles such as rapid knocks, and clear active/queued dialogue during a full game restart.
# res://autoload/GameState.gd - Store global state flags, power mode, active character, pause and journal-open state, signals, the trap-triggered permanent power-off lock, and reset all gameplay state to defaults for a full restart.
# res://autoload/InputRouter.gd - Convert input actions into global signals, block gameplay Use/Special/Swap/Journal requests while the active character is in a special action, include throttled joypad-motion repeats for UI directional navigation, and track last-used keyboard/gamepad family for HUD action key labels.
# res://autoload/InteractableManager.gd - Fallback special-action handling when no interactable is in range, including clearing pending fallback actions during a full restart.
# res://autoload/NotesManager.gd - Load notes catalog, track collected notes, expose collected note lookup/catalog-fill helpers, cache journal text, and clear collected notes during a full restart.
# res://autoload/SceneRouter.gd - Route scene changes by path, clear pause state before handoff, threaded-preload upcoming scenes during cutscenes/victory delay, and own the post-victory delay before outro-video routing.
# res://autoload/SettingsManager.gd - Persist and apply player settings for audio volumes/mute with per-bus max dB ceilings and unsaved live audio preview support, exclusive fullscreen/windowed display mode with centered windowed resolution, global Alt+Enter fullscreen toggle, Fast/Balanced/Nice performance presets, FPS visibility, and keyboard/gamepad binding overrides, preserving project defaults for control reset.
# res://data/dialogue.json - Dialogue text keyed by id for DialogueManager.
# res://data/notes.json - Notes catalog keyed by id for NotesManager.
# res://data/audio/sfx/ - Source WAV sound effects for gameplay and UI audio.
# res://data/audio/sfx/generator_quit.wav - Generator shutdown sound effect.
# res://data/audio/sfx/generator_run.wav - Generator running loop or sustained sound effect.
# res://data/audio/sfx/generator_start.wav - Generator successful startup sound effect.
# res://data/audio/sfx/generator_start_fail.wav - Generator failed startup sound effect.
# res://data/audio/sfx/walk.wav - Shared two-second walking sound effect containing two one-second loops, intended for every walking direction on both characters.
# res://data/video/ - Source MP4/H.264 cutscene videos used by the GDE GoZen-backed intro/outro playback scenes.
# res://addons/gde_gozen/ - GDE GoZen FFmpeg-backed video playback addon used by intro/outro MP4 cutscene scenes; export builds must ship external MP4 files beside the executable and include the platform native library.
# res://data/res/SelectedLabel.tres - LabelSettings asset for selected UI text.
# res://data/res/StaticLabelBlack.tres - LabelSettings asset for black UI text.
# res://data/res/StaticLabelWhite.tres - LabelSettings asset for white UI text.
# res://data/res/InputTheme.tres - Legacy/editable input theme resource; current audio volume options no longer use SpinBox because their values are display-only Button controls.
# res://default_bus_layout.tres - Audio bus layout defining SFX, UI, Music, and +3 dB Video buses routed through Master.
# res://project.godot - Project configuration for main scene, autoloads including SettingsManager, separated gameplay movement (`move_*`) and UI navigation (`ui_*`) input actions, display stretch, and the default audio bus layout.
# res://scenes/levels/GasStation.tscn - Main playable gas station scene, including world layers, camera audio listener, playable characters under the Y-sorted `Characters` parent, their non-positional WalkAudio nodes, spatial generator/flicker/knocking/pumps/slime AudioStreamPlayer2D nodes.
# res://scripts/gameplay/CharacterAudio.gd - Character-owned audio component that resolves child or legacy sibling AudioStreamPlayer nodes, configures looped walking WAV playback, plays a random configured Damage*Audio clip on nonlethal damage, and exposes walking plus named one-shot sound playback with optional playback during pause.
# res://scripts/gameplay/CharacterBase.gd - Base character stats, health, damage, and death-sequence signals, special action timing, dialogue anchor routing, and non-pushing movement resolution.
# res://scripts/gameplay/CharacterManager.gd - Manage active character control, location-based body collision rules, current camera layer, and intro dialogue.
# res://scripts/gameplay/CheatManager.gd - Runtime debug-build cheat shortcuts for skipping Gas Station puzzle progression, filling the journal for audio/page testing, and forcing generator power during testing.
# res://scripts/gameplay/DebugManager.gd - Debug overlay owner, one-second FPS/average/worst-frame telemetry sampler, and editor scene setup helper with inspector toggles for previewing power art state, generator sprite visibility/position, and full cellar door open/closed state including open-door wall collision outside gameplay.
# res://scripts/gameplay/DialogueTriggers.gd - Trigger dialogue and notes on GameState flags, including active-character-aware anchor routing for the GSA discovery line.
# res://scripts/gameplay/ElevationArea.gd - Apply configurable visual Y elevation offsets to CharacterBody2D nodes inside area bounds.
# res://scripts/gameplay/ExposedWiresInteractable.gd - Handle exposed wires shock, fix gating, wire trap center-point electrocution/pushback, wire trap sprite/mask visibility, layer-gated exposed wire thrum/sparks with spatial high/low spark audio, and wire trap glow/zap effects with trap zap synced to spark bursts.
# res://scripts/gameplay/AlphaMaskArea.gd - Apply an invisible Sprite2D alpha mask to character and slime visuals, cache maskable bodies and transformed trigger geometry instead of traversing the scene per physics frame, use anchor-point checks by default with optional Area2D body overlap for slime on specific masks such as the wire trap, forward the active slime mask to the trail renderer, and support runtime enable/disable for state-swapped masks.
# res://scripts/gameplay/AmbienceAudio.gd - Non-spatial looping ambience AudioStreamPlayer controller that plays on the SFX bus only while its configured LayerController view layer is active.
# res://scripts/gameplay/FlickerLight.gd - Randomly toggle a Sprite2D on/off with burst flickers and spatial on/off audio to simulate a fluorescent light, only while grid power is on and its configured view layer is active.
# res://scripts/gameplay/FollowCamera.gd - Follow the active character and add camera shake/jerk effects.
# res://scripts/gameplay/GasStationPuzzle.gd - Gas Station puzzle progression, including GSA closet repair state, direct closed/open cellar door wall collision switching, closed/open cellar door mask switching, and breaker lockout after permanent power-off.
# res://scripts/gameplay/GeneratorInteractable.gd - Generator-specific interact, fix, examine, power toggle logic, permanent disable state, spatial fail/start/run/stop audio sequencing, and layer-gated GennySprite alpha/position startup/running/off visual state.
# res://scripts/gameplay/Interactable.gd - Base interactable logic for range checks, standard/special-only requirements, special actions, cancellation cleanup, repeat/fail, and permanently disabled interaction gates.
# res://scripts/gameplay/Inventory.gd - Track item ownership, play SFX-bus pickup feedback on successful grants, emit change signals, and clear inventory during a full game restart.
# res://scripts/gameplay/KnockingArea.gd - Spawn repeated detached knock bubbles from a dialogue anchor at timed spacing, cycle one spatial knock audio per knock set, and trigger a journalist line until GSA is discovered.
# res://scripts/gameplay/LayerController.gd - Track current camera/view layer, emit visibility/layer-change callbacks, and keep the exterior-only slime on the normal world collision layer without duplicating static geometry.
# res://scripts/gameplay/LayerTrigger.gd - Teleport characters between separated exterior/interior/cellar spaces and update their view layer.
# res://scripts/gameplay/MetalPostInteractable.gd - Control metal post electrocution and shocks.
# res://scripts/gameplay/MusicManager.gd - Non-spatial music controller that shuffles the seven song OGG files, plays them through an internal always-processing AudioStreamPlayer on the Music bus so music continues during pause, and exposes a 0-1 volume slider for future audio settings.
# res://scripts/gameplay/NoteInteractable.gd - Add a note and optional dialogue on interact or special success (reusable).
# res://scripts/gameplay/PowerSystem.gd - Apply power mode effects to pumps, looping spatial pump audio, spatial grid on/off transition audio, lights, slime, level background sprites, and lit/dark cellar door-open overlay visibility.
# res://scripts/gameplay/PumpsArea.gd - Shared pump-region trigger that preserves the Journalist's one-time entry dialogue and acts as a Journalist Examine area for adding the Slime Monster journal note.
# res://scripts/gameplay/GasSpillEffect.gd - Detect when the slime enters or exits the pump gas spill polygon using cached transformed geometry and toggle the slime's spill-only ripple visuals.
# res://scripts/gameplay/SlimeBody.gd - Track slime layer location like characters, drive SlimeSprite animations/audio including death-animation completion signaling, switch circle collision radius between normal and follow states, apply Fast/Balanced/Nice visual performance profiles, spawn pool-clipped ripple bursts, configure and drive the outside-pool trail, and resolve movement collisions without pushing bodies.
# res://scripts/gameplay/SlimePoolRipple.gd - Render a pool-clipped grayscale ripple by expanding only the current slime frame's semi-transparent edge pixels inside the gas spill polygon, then fade and free itself with throttled shader uniform updates.
# res://scripts/gameplay/SlimeTrailEmitter.gd - Active trail helper that samples SlimeBody's sprite-edge trail contact point, tracks movement-facing changes without treating contact-point jumps as travel, requires accumulated body travel to reach stamp spacing before depositing or compounding field values, resets accumulation on blocked movement, caps deposits per physics update, and interpolates outside-pool movement into SlimeTrailManager.
# res://scripts/gameplay/SlimeTrailManager.gd - Active field-backed world-space slime trail prototype that deposits gasoline residue into a persistent scalar field, caps repeat pooling and skips saturated deposits, tracks consecutive stamp directions for turn pooling, compacts decaying active cells in one linear pass, sleeps when no decay/upload/cooldown work remains, recenters the floating field canvas using reusable double buffers plus a short anti-thrashing cooldown, throttles decay/upload work, and displays it with a cartoon oil shader that can optionally apply the slime's active AlphaMaskArea mask.
# res://scripts/gameplay/SlimeSystem.gd - Exterior-only slime AI state machine with creep and pursuit behavior, collision-tested unstuck waypoints, power-mode targeting contexts, trap attraction/death, and handoff to SceneRouter once the slime death animation completes.
# res://scripts/gameplay/TopDownController.gd - Gameplay `move_*` input polling with small off-axis cardinal snapping for analog/gamepad movement, walking-state and special-action audio delegation including one-second audio truncation for failed Examine/Repair attempts, non-cancellable special-action movement lock, choose examine/repair special-action animations with safe fallbacks, and expand a direction-rotated CorpsePerimeter during death animations to push the slime outward with collision-safe movement before signaling for the death menu two seconds after animation completion.
# res://scripts/gameplay/WireCoilInteractable.gd - Reveal WireCoilSprite at full opacity and slowly pulse its HSV value between 0.7 and 0.9 when the slime-trap plan unlocks the pickup, grant the wire coil item and journal entry, and hide/disable the pickup after use.
# res://scripts/gameplay/ZOrderManager.gd - Toggle managed sprite draw order above/below active character based on anchor overlap with sprite bounds.
# res://scripts/ui/JournalScreen.gd - Control-based journal overlay that paginates collected notes into the journal label, manages prev/next page buttons, and plays non-positional UI bus page-turn audio.
# res://scripts/ui/MenuHeadingLabel.gd - Reusable tool RichTextLabel heading formatter that takes raw text such as "AUDIO", applies shared Libre Baskerville bold FontVariation styling, centers the text, auto-fits the control width, and exposes first-letter/rest-letter sizes, embolden amount, font, and color in the inspector.
# res://scripts/ui/MenuScreen.gd - Pause/death/solved menu behavior including independent death/solved stamps and audio, full Retry reset to the configured gameplay scene, Options navigation, settings controls, runtime input binding UI, and SFX-bus menu feedback.
# res://scenes/ui/TitleScreen.tscn - Inspector-configurable title scene with Play/Options/Exit menu and editable title Options panel/container layout including a styled TitleOptionsPanel; Play resets runtime state and routes to the configured intro video scene.
# res://scripts/ui/TitleScreen.gd - Title menu controller for Play/Options/Exit input, startup focus, runtime-state reset, intro scene routing, and SettingsManager-backed options row behavior populated into the scene-authored Options panel while taking row/scroll styling from TitleOptionsPanel.
# res://scripts/ui/TitleOptionsPanel.gd - Inspector-facing title Options panel skin script exposing panel background/border, option row background/selection, row width, and scrollbar color/spacing settings.
# res://scenes/ui/VideoScreen.tscn - Reusable fullscreen GDE GoZen VideoPlayback scene with black background, aspect-ratio scaling, skip input, and configurable next-scene routing.
# res://scripts/ui/VideoScreen.gd - Reusable cutscene video controller that resolves inspector-assigned MP4 paths through GDE GoZen, using `res://data/video/` in editor and configurable executable-relative external search folders in release, routes cutscene audio to the +3 dB Video bus, preloads its configured next scene during playback, advances on video end/skip, and can advance when the file is missing during setup.
# res://scenes/ui/IntroVideo.tscn - Intro cutscene wrapper configured to load `res://data/video/hh_intro.mp4` and advance to GasStation gameplay.
# res://scenes/ui/OutroVideo.tscn - Outro cutscene wrapper configured to load `res://data/video/hh_outro.mp4` and advance back to the title screen.
# res://scenes/ui/TextBubble.tscn - Editor-tunable dialogue bubble template whose PanelContainer fits its text with internal style margins, instantiated by DialogueManager.
# res://scripts/ui/TextBubble.gd - Floating retargetable text bubble display that follows its target, fits short text exactly, wraps at a configurable maximum width, bottom-anchors and camera-clamps the complete panel, fades, and expires.
# res://scripts/ui/UIManager.gd - HUD portrait masking, shader-driven character life bars, action button hookups/background hover-click-key feedback, action key labels from current keyboard/gamepad bindings, special-action icon/background radial progress, delayed death-menu activation after character death sequences, rich journal update popup, and journal toggles with non-positional UI bus open/close audio.
